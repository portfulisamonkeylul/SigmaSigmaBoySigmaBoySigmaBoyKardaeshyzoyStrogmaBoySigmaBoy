# We ignore the config so that people can set usernames and local paths without pushing them to everyone else.
settings.cfg


# This is all Kerbal Space Program specific. Delete if your game uses .sfs or .loadmeta files
# This optimizes storage use by not pushing backups or quicksaves to remote.
*.sfs
*.loadmeta

!persistent.sfs
!persistent.loadmeta
!*/Ships/**/*.loadmeta
!*/Subassemblies/*.loadmeta